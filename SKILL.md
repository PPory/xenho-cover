---
name: xenho-cover
description: 把文章、小红书笔记、公众号推文、X 帖子或一个主题变成封面图像提示词，风格来自自建风格库（Anthropic Research 编辑风、Notion 手绘信息图等；纯提示词输出，用户自己贴到 GPT Image-2 出图）。触发词：「配个封面」「封面图」「封面提示词」「Claude 风格封面」「Notion 风格封面 / 信息图」「公众号头图」「小红书封面」。与 poster-design 的区别：那个是 Mondo 及传奇设计师风格海报且可直接出图，本 skill 用自建风格库且只产提示词；与 claude-design 的区别：那个产 HTML/CSS 设计稿，本 skill 产图像生成提示词。用户明确点名设计师风格海报或 HTML 设计稿时不要用本 skill。
---

# xenho-cover

## 核心规则

`xenho-cover` 是封面提示词生成工作流，不拥有风格。从 `styles/` 库选定**恰好一个**风格原子，把它编译进封面形状，产出**一份完整独立的图像生成提示词**。

最终提示词不是「封面指令 + 风格原文」的拼接。必须把风格锚点改写融合进 `references/cover-prompt-blueprint.md` 定义的结构，让每个封面决定（标题、主体、背景、小字）都通过该风格的视觉语言实现。

## 资源

- `references/style-catalog.md`：风格菜单和平台推荐，最先读
- `references/cover-prompt-blueprint.md`：最终提示词的结构，编译前必读
- 选定风格后读 `styles/{style-id}/META.md`（结构化锚点）+ `styles/{style-id}/STYLE.md`（视觉语言）
- `refs/`：开发期参考图，运行时不加载

## 工作流

### 1. 提炼派生字段

从来源内容提取：

- 标题三层：A 层（短而有力的视觉标题）/ B 层（完整标题）/ C 层（副标题或小字，可留空）
- 一句话语境摘要、视觉主体、视觉隐喻、情绪、禁用元素
- 长文只进派生字段。原文正文不进提示词、不进画面。

**分支：自有产品发布**

来源内容是「介绍我自己做的 X」（产品、工具、项目的上线或更新）时，标题分层反过来定：

- A 层 = 产品名本身，做全画面最大的视觉主体，**不是**从正文提炼的金句
- B 层的金句降为 C 层副标题一行（例：`Typeset.` 为主，「少折腾排版，多琢磨想说的话」为副）
- 产品名在成品提示词里必须给出精确拼写与大小写，并写进终检标准逐字核对
- 产品名是拉丁字母时优先做主体巨字——顺带绕开汉字写错这个最高频翻车点
- 选风格时优先「标题即主体」型（`bw-type-specimen`、`film-poster-icon`、`anthropic-research`），避开主体被插画或摄影占满、留给名字位置不够的风格

### 2. 门控：确认平台

平台与比例映射：

- 小红书：`3:4`
- 公众号：`2.35:1`
- X：`5:2`
- 自定义：完全按用户给的比例

用户没给平台也没给比例时，**停下来问平台，不要先生成**。用户已给平台/比例、或明说「auto / 你定」时直接继续。

风格按 `references/style-catalog.md` 的「风格确认规则」处理：用户点名直接用；未点名时按内容推荐一个，在平台确认的同一条消息里顺带给出（不额外增加提问轮次）。

### 3. 编译提示词

1. 读 blueprint + 选定风格的 `META.md` 和 `STYLE.md`
2. 按 blueprint 结构编译：填实所有占位符，把 `style_anchors`、`cover_shape_adaptation`、`must_preserve`、`avoid_when_applying_to_cover` 改写成本次封面的具体画法
3. 目标出图工具是 **GPT Image-2**，提示词默认输出中文（已验证 GPT Image-2 对中文提示词和中文字形的还原良好）；用户明确要英文时才输出英文版

### 4. 输出

- 把完整提示词用代码块直接贴在会话里（主交付物，方便一键复制）
- 同时存档到用户 home 目录下的 `.claude/data/xenho-cover/{YYYYMMDD}-{slug}.md`（解析成当前系统的绝对路径，目录不存在先创建）
- 回复末尾一句话说明：用了什么风格、什么比例、存档在哪

## Gotchas

- **不许拼接**：把 `STYLE.md` 原文整段贴在提示词后面当第二部分是最常见的失败。必须改写融合，成品里认不出原文段落才算合格。
- **汉字正确性必须写进提示词**：图像模型写错中文字是最高频翻车点，每份提示词都要有「汉字必须正确、完整、清晰可读」的硬性要求。
- **隐喻禁直译**：AI 不画机器人，网页不画电脑，效率不画时钟齿轮。从内容的核心张力提炼抽象隐喻。
- **只要一张成图**：提示词里明确禁止多方案、网格图、对比图。
- **品牌按「是谁的」分两类，不是一刀切**：
  - **别人的**——风格原作方（Claude/Anthropic 等）的 logo 与字样，以及文章里提到的第三方公司名、产品名、真实人名，一律不入画；标题含它们时先去品牌化改写（例：「Claude 的记忆系统」→「会删除的记忆」）。风格借鉴 ≠ 品牌冒用。
  - **作者自己的**——内容出现「我做的」「我搓了一个」「自研」「刚上线」「我们团队的」这类第一人称所有权表述时，那个名字**不但可以入画，而且就是封面主体**，走工作流 1 的「自有产品发布」分支。给自己的产品做发布封面却把名字抹掉，等于把发布图做成了随笔配图——真实翻过一次车。
  - 归属拿不准时按「别人的」处理，并在回复里问一句。
- **封面不是详情页**：文字总量硬上限 = 一个主标题 + 至多一行副标题 + 至多两处点缀（栏目标签 / 网址 / 样张行 / 期号数字，任选）。风格原子里的说明块、小字信息块、样张行是**可选槽位，不是必填清单**——照单全收就会把封面写成产品详情页（真实翻车：一版封面同时塞了三行说明块 + 网址 + 样张行，读者第一眼不知道该看哪）。整段说明性句子永远不入画，那是正文的活。用户说「文字少一点」时，砍的是点缀，不是副标题。
- **占位符清零**：成品提示词里残留任何 `{...}` 或 `{{...}}` 都算失败。
- **平台未知就停**：不要为了省一轮对话先猜平台生成，比例错了整张图作废。
- **气质需求撞上库的边界时，说明冲突、给替代，绝不改风格原子来凑**：用户描述的气质可能组合出库内不存在的交集（真实案例：「更有温度和科技感」——`cyber-industrial-panel` 是严格单色灰阶、风格文件明写「避免温暖人文类内容」，给它加暖色就是破坏风格原子，出图必然四不像）。正确做法是**先说清哪一半给不了、为什么**，再推荐最接近的现有风格并讲明取舍（该案例改用 `anthropic-research`：暖底解决温度，研究报告式线稿承接理性技术感），最后告诉用户如果优先另一半可以换哪个。风格原子只在**照着 `refs/` 参考图修正**时才改，不为单次需求临时改。
- **改风格前先看 `refs/`，但只抄设计语言、不抄题材**。这条有两半，两半都翻过车：
  - **抄不够**：凭印象给 `highlight-archive-type` 加了「只划关键词高亮」和「条端参差不齐」两条规则，参考图里根本没有（原作是每行整行全高亮、条端是干净矩形）；纸底也定成了暖米色，而参考图是冷灰白。规则一旦偏离参考图，出图必然偏离风格。
  - **抄过头**：`cinematic-photo-editorial` 的参考图恰好是一张潜水海报，于是「水下」「游鱼」「礁石」「水体」被写进了色板说明、安静区、前景剪影和场景取材四处，导致不管什么文章都往水里拍。
  - 判据：参考图提供的是**构图、排版、色彩分级、信息层级、质感**；**题材永远由文章决定**。写风格原子时，任何一条规则如果换一个题材就不成立，它就是题材泄漏，必须改写成场景无关的表述。
